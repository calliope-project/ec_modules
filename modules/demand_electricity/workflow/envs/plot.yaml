# Adding this because dependencies in geo.yaml are very old
# And cause a problem with matplotlib=3.7.1
# Once geo.yaml is updated, this can env can probably be removed
name: plot
channels:
    - conda-forge
dependencies:
  - python=3.11
  - pandas
  - geopandas
  - matplotlib
